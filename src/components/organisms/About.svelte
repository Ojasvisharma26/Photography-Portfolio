<script>
	import RichPresence from '../molecules/RichPresence.svelte';
	import Tooltip from '../atoms/Tooltip.svelte';

	// i didnt write this idk
	let getAge = () => {
		let birthDate = new Date('2006/10/26');
		const ageMs = Date.now() - birthDate.getTime();
		const preciseAge = (ageMs / 31536000000).toFixed(10);
		return preciseAge;
	};

	let age = getAge();
	setInterval(() => {
		age = getAge();
	}, 1000);
</script>

<section id="about" class="wrapper">
	<div>
		<RichPresence />
	</div>
	<div class="text">
		<h2>bio</h2>
		<p>
	Hey there, I'm <Tooltip tip="short for Ojasvi"><span>OJ!</span></Tooltip> :] & 
	I’m <Tooltip tip={age}><span>{Math.floor(Number(age))}</span></Tooltip> year old,
	a first-year student at 
	<Tooltip tip="B.com Programme"><span>Zakir Husain Delhi College</span></Tooltip>.
	I work across sketching, photography, video editing, music, and creative storytelling,
	exploring moods and everyday moments through different mediums.

	Beyond art, I’m a national-level 
	<Tooltip tip="India"><span>Tennis Player</span></Tooltip>,
	which has shaped my discipline and focus.
	I also explore coding and web development, blending creativity with logic.

	Always learning and pushing my limits — creatively and personally.
</p>
	</div>
</section>

<style lang="scss">
	@import '../../styles/mixins.scss';

	section {
		margin-bottom: 6rem;
		display: grid;
		gap: 4.5rem;
		grid-template-columns: 1fr 1fr;
		align-items: center;
	}

	.text {
		position: relative;
		line-height: 1.75rem;
	}

	span {
		font-weight: 400;
		font-family: var(--font-two);
		font-size: 0.9rem;
		background-color: var(--elevation-one);
		border-radius: 7px;
		color: var(--accent);
		padding: 0.2rem 0.5rem 0.2rem;
		width: fit-content;
	}

	a {
		text-decoration: none;
	}

	.text::before {
		@include outlineText(
			$content: 'OJ',
			$translateX: 97%,
			$translateY: -5%,
			$fontSize: 300px,
			$opacity: 0.22
		);
	}

	h2 {
		display: none;
		margin-top: 1rem;
	}

	@media (max-width: 868px) {
		section {
			display: flex;
			flex-direction: column;
			align-items: normal;
		}

		h2 {
			display: block;
			margin-bottom: 1rem;
		}
	}
</style>
